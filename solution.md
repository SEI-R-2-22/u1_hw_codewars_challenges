## Return Negative

```js

const returnNeg = (num) => {
    if(num > 0){
        num = num - (num * 2);
    }else{
        return num
    }
    return num
}


```

## Sum of Positive

```js
    const sumPos = (arr[]) => {
        let sum = 0;
        for(let i=0;arr.length;i++){
            if(i > 0){
                sum += arr[i];
            }else{
                sum += o;
            }
        }
        return sum;
    }
```

## Function 2

```js
    const squareNum = (num) =>{
        retrun (num * 2)
    }
```

## Sum Arrays

```js
    const returnNum = (arr[]) => {
        let sum = 0;
        if(arr.length > 0){
            for(let i=0;i<arr.length; i++){
                sum += arr[i];
           }
        }else{
            return 0;
        }
        
    }
```

## Reversed Strings

```js
    const reverseString(str){
        let stringArray = [];
        let conString = "";
            for(let i=0; i < str.length; i++){
                stringArray.push(str.chatAt[i]);
            }
            stringArray.reverse();
            for(let x=0 ; x < stringArray.length ; x++){
                conString += stringArray[x];
            }
            return conString;
    }
```
